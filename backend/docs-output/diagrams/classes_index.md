# Class Relationships: index

Inheritance and composition between classes. 3 class(es) with no detected relationship are omitted from this view.

```mermaid
classDiagram
  class EmbeddingCache
  EmbeddingCache *-- Path
```
